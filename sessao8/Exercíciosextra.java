package sessao8;

public class Exercíciosextra {
    /* Crie uma função calcular() que recebe dois números e um operador (+, -, *, /) e retorna o resultado da operação.
    */
   public static double calcular(double a, double b, char operador) {
    if(operador == '+'){
    return a + b;
} else if(operador == '*') {
    return a * b;

}

